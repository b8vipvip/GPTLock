{
  "name": "gptlock-extension",
  "version": "0.3.5",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --check background.js && node --check content.js && node --check popup.js && node --test tests/*.test.mjs"
  }
}
